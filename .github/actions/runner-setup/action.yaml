name: 'Runner setup'
runs:
  using: 'composite'
  steps:
      - uses: actions/setup-node@v6
        with:
          node-version: 20
          cache: 'pnpm'

      - uses: pnpm/action-setup@v6

      - name: Install dependencies
        shell: bash
        run: pnpm install
