import { token } from '@kda-community/kode-ui/styles';
import { style } from '@vanilla-extract/css';

export const noTransactionsTitleClass = style({
  textAlign: 'center',
  color: token('color.text.gray.default'),
});
