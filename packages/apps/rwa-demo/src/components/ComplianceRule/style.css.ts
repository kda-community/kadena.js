import { style } from '@kda-community/kode-ui';
import { atoms } from '@kda-community/kode-ui/styles';

export const complianceRuleClass = style([
  atoms({
    borderRadius: 'sm',
    border: 'hairline',
    paddingInline: 'n4',
    paddingBlock: 'n3',
  }),
  {},
]);
